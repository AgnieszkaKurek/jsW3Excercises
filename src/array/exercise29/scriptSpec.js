import {
   fillArrayWithValues
} from './script';

describe("Fill an array with values (numeric, string with one character) on supplied bounds: ", (() => {
    it("", () => {
        let newArray = fillArrayWithValues();
        expect(newArray).toEqual([]);
    });
    it("", () => {
        let newArray  = fillArrayWithValues();
        expect(newArray).toEqual([]);
    });
    
}));